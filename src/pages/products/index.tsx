import { ProductsDataPage } from "@/features/products/features";
import { DataLayout } from "@/shared/layouts";

const ProductsPage = () => {
  return (
    <DataLayout>
      <ProductsDataPage />
    </DataLayout>
  );
};

export default ProductsPage;
